#pragma once

#include <iostream>
#include <string>

struct vowels {
	int numA;
	int numE;
	int numI;
	int numO;
	int numU;
	vowels() : numA(0), numE(0), numI(0), numO(0), numU(0) {};
	//vowels(vowels v) : numA(v.numA), numE(v.numE), numI(v.numI), numO(v.numO), numU(v.numU) {};
};